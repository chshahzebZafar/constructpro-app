/**
 * Tool UI feature flags. Keep upload/picker code paths in screens; toggle here for releases.
 * Set to true when photo upload is ready to ship.
 */
export const TOOL_PHOTO_UPLOAD_ENABLED = false;
