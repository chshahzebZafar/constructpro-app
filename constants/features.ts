/**
 * Flip these when re-enabling sign-in options in a future release.
 * Google Sign-In requires OAuth client + SHA config for Play Store builds.
 */
export const ENABLE_GOOGLE_SIGN_IN = false;

/** “Continue without signing in” on the login screen (was __DEV__-gated). */
export const ENABLE_DEV_PREVIEW_LOGIN = false;
