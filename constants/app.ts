import Constants from 'expo-constants';

export const APP_VERSION = Constants.expoConfig?.version ?? '1.0.0';

/** Shown on Support / About — replace with your real support inbox when ready. */
export const SUPPORT_EMAIL = 'support@constructpro.app';
